// tests/test_math_functions.cpp
#include <gtest/gtest.h>

TEST(MathFunctions, Addition) {
    EXPECT_EQ(2+3, 5); // Assuming you have an `add` function in your_library_header_files.h
}

TEST(MathFunctions, Subtraction) {
    EXPECT_EQ(5-3, 2); // Assuming you have a `subtract` function
}
